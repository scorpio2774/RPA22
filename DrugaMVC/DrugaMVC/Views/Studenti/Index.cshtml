@model IEnumerable<DrugaMVC.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Age)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.StudentId }) |
            @Html.ActionLink("Potrobnosti", "Details", new { id=item.StudentId }) |
            @Html.ActionLink("Zbriš ta jajc", "Delete", new { id=item.StudentId })
        </td>
    </tr>
}

</table>
